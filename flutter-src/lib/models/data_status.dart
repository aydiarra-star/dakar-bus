/// Dakar Bus - Groupe 11
/// DataStatus : contrat validé Groupe 10H-C, ne pas modifier
/// scheduled = horaire réellement sourcé
/// live = donnée temps réel réellement sourcée
/// unknown = aucune donnée temporelle exploitable

enum DataStatus {
  scheduled,
  live,
  unknown,
}
